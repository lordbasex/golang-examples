[general]
enabled = yes           ; Habilita el servidor HTTP
bindaddr = 0.0.0.0      ; Escucha en todas las interfaces
bindport = 8088         ; Puerto HTTP para ARI y otros endpoints
tlsenable = no          ; TLS deshabilitado para pruebas (en producción, conviene activarlo)
