{
  "name": "certgen-wasm-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cert generator demo: runs entirely in the browser via Astro + Go WebAssembly + Service Worker (go-wasm-http-server).",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.6.0"
  }
}

